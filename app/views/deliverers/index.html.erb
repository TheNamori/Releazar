<p id="notice"><%= notice %></p>

<h1>Listing Deliverers</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Phone</th>
      <th>Blocked</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @deliverers.each do |deliverer| %>
      <tr>
        <td><%= deliverer.name %></td>
        <td><%= deliverer.phone %></td>
        <td><%= deliverer.blocked %></td>
        <td><%= link_to 'Show', deliverer %></td>
        <td><%= link_to 'Edit', edit_deliverer_path(deliverer) %></td>
        <td><%= link_to 'Destroy', deliverer, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Deliverer', new_deliverer_path %>
