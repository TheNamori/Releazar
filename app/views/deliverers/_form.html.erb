<%= form_for(@deliverer) do |f| %>
<% if @deliverer.errors.any? %>
<div id="error_explanation">
  <h2><%= pluralize(@deliverer.errors.count, "error") %> prohibited this deliverer from being saved:</h2>

  <ul>
    <% @deliverer.errors.full_messages.each do |message| %>
    <li><%= message %></li>
    <% end %>
  </ul>
</div>
<% end %>

<div class="field">
  <%= f.label :name %><br>
  <%= f.text_field :name %>
</div>
<div class="field">
  <%= f.label :phone %><br>
  <%= f.text_field :phone %>
</div>
<div class="field">
  <%= f.label :blocked %><br>
  <%= f.text_field :blocked %>
</div>
<div class="actions">
  <%= f.submit %>
</div>
<% end %>